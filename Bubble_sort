#include <iostream>
#include <vector>
using namespace std;
vector<int> a;
int main() {
	int n;
	cin >> n;
	for (int i = 0; i < n; i++) {
		int t;
		cin >> t;
		a.push_back(t);
	}
	for (int i = 0; i < n - 1; i++) 
		for (int j = 0; j < n - 1 - i; j++) 
			if (a[j] > a[j + 1]) 
				swap(a[j], a[j + 1]);
	for (int i = 0; i < n; i++) 
		cout << a[i] << " ";
	return 0;
} 
